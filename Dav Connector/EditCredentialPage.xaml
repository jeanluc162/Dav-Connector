<Page
    x:Class="Dav_Connector.AddCredentialPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dav_Connector"
    xmlns:VM="using:Dav_Connector.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <VM:EditCredentialPageViewModel />
    </Page.DataContext>
    <StackPanel Orientation="Vertical" Padding="5">
        <TextBlock>Name</TextBlock>
        <TextBox Text="{Binding Name, Mode=TwoWay}"></TextBox>
        <TextBlock>Username</TextBlock>
        <TextBox Text="{Binding UserName, Mode=TwoWay}"></TextBox>
        <TextBlock>Password</TextBlock>
        <PasswordBox Password="{Binding Password, Mode=TwoWay}"></PasswordBox>
        <Button HorizontalAlignment="Stretch" Name="SaveButton">Save</Button>
    </StackPanel>
</Page>

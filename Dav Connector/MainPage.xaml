<Page
    x:Class="Dav_Connector.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dav_Connector"
    xmlns:VM="using:Dav_Connector.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Name="PageElement">
    <Page.DataContext>
        <VM:MainPageViewModel />
    </Page.DataContext>
    <Grid Padding="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Button Name="AddCredentialButton" Command="{Binding EditAccountCommand}" Grid.Column="1" Grid.Row="0">
            + Add Account
        </Button>

        <ListView Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" ItemsSource="{Binding Accounts}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" Grid.Column="0">
                            <TextBlock Text="{Binding Name}" FontStyle="Oblique" FontWeight="ExtraBold" FontSize="18"></TextBlock>
                            <TextBlock Text="{Binding Url}" FontStyle="Italic" FontWeight="Bold" FontSize="14"></TextBlock>
                            <TextBlock Text="{Binding UserName}" FontStyle="Italic" FontWeight="Bold" FontSize="14"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Column="1">
                            <Button HorizontalAlignment="Right" VerticalAlignment="Stretch" Command="{Binding DataContext.StartSyncCommand, ElementName=PageElement}" CommandParameter="{Binding Id}">Sync Now</Button>
                            <Button HorizontalAlignment="Right" VerticalAlignment="Stretch" Command="{Binding DataContext.EditAccountCommand, ElementName=PageElement}" CommandParameter="{Binding Id}">Edit</Button>
                            <Button HorizontalAlignment="Right" VerticalAlignment="Stretch" Command="{Binding DataContext.DeleteAccountCommand, ElementName=PageElement}" CommandParameter="{Binding Id}">Delete</Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
